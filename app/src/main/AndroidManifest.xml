<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Location — both precisions; fine gives the best GPS fix -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WorkAssistance"
        tools:targetApi="31">

        <!--
            Google Maps Android SDK API key.
            Value is injected from local.properties via manifestPlaceholders in build.gradle.kts.
            To set your key: add  MAPS_API_KEY=your_key_here  to local.properties (never commit that file).
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".ui.auth.LoginActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />

        <activity
            android:name=".ui.site.SiteManagementActivity"
            android:exported="false" />

        <activity
            android:name=".ui.site.ProofOfWorkActivity"
            android:exported="false" />

        <activity
            android:name=".ui.site.SiteRequestsActivity"
            android:exported="false" />

        <activity
            android:name=".ui.site.SiteDetailActivity"
            android:exported="false" />

    </application>

</manifest>
